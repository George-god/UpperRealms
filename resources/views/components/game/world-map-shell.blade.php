{{--
  Reference layout for a node-based realm map (Tailwind + SVG).
  Live classic page: legacy/pages/world_map.php
--}}
@props(['regions' => []])

<div class="rounded-2xl border border-indigo-500/20 bg-[rgba(20,25,45,0.75)] p-4 backdrop-blur-xl">
    <p class="text-center text-sm text-slate-500">{{ __('Map shell — use legacy world_map.php for wired exploration.') }}</p>
</div>
