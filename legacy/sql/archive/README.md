# Archived SQL snippets

These files are optional or legacy snippets layered on the main dump. **Use the root [`database_full.sql`](../../database_full.sql) for new database installs** (core + bloodlines + caves + artifacts in one file).

| File | Notes |
|------|--------|
| Other files here | Legacy modular scripts kept for reference or incremental migrations (run only after `database_full.sql` if you are not using the root dump). |
